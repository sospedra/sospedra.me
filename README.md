[sospedra.me](https://sospedra.me)
==================================

The Rubén Sospedra's website source code.
Javascript hacker.
Fullstack engineer.

Next.js 16 app router. Node 22 and pnpm 10 (`asdf install`).
`pnpm dev` to develop. `pnpm build` to build. `pnpm test` runs tsc and eslint.
Papers live in `content/papers/<slug>/` as mdx plus metadata.json.

*Handcrafted with <3 in Barcelona*
