name: publish-logatim

on:
  push:
    branches: [main]
    paths: ["packages/logatim/**"]

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"
      - run: pnpm install --frozen-lockfile
      - name: compare against the published version
        id: guard
        run: |
          local=$(node -p "require('./packages/logatim/package.json').version")
          remote=$(npm view logatim version 2>/dev/null || echo none)
          echo "changed=$([ "$local" != "$remote" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
      - name: gates
        if: steps.guard.outputs.changed == 'true'
        run: |
          pnpm --filter logatim run lint
          pnpm --filter logatim run typecheck
          pnpm --filter logatim run test
      - name: publish
        if: steps.guard.outputs.changed == 'true'
        run: pnpm --filter logatim publish --no-git-checks
