# Codex order — scavenger master r4 — fluorescent depth light, flat counter, deeper keeper

You are an image-generation subagent. Execute exactly this and nothing else.

Attached:
- Image 1 = the angle law diagram: GREEN construction only (front
  rectangle + one thin top band, vertical sides); RED isometric
  forbidden.
- Image 2 = the WORLD-CAMERA GOSPEL (the approved games stall): copy
  the camera, the flatness, the outline weight, and the paint manner.
  Copy ZERO content from it.
- Image 3 = YOUR OWN RENDER, the round-3 winner, the DIRECTION GOSPEL:
  copy its keeper identity, its flat wallet, its counter item look,
  its stock density and gradient. REJECTED from it, fix exactly these:
  (a) the counter drew as a 3D TABLE with VISIBLE LEGS and a deep
  top: the flat storefront elevation was lost; (b) the bulb string;
  (c) the "scavenger" sign plank; (d) the hanging fish plaque; (e)
  the flying drone; (f) the wooden shelf unit; (g) the keeper too
  visible: he sits deeper and darker now. NEVER copy its layout
  verbatim and NEVER draw any attachment into the output.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1536x1024.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/r4-j1/gen-scavenger-master.png
then print GENERATED=<that path>. No post-processing, no repo edits.
