# Codex order — scavenger pipe smoke — 3 steam cells on chroma

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = an approved steam sprite from this game: the exact
style authority for wisps: flat grey pixel chunks. Copy its manner.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1536x1024.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/anim4-smoke/gen-smoke-sheet.png
then print GENERATED=<that path>. No post-processing.

The ENTIRE canvas is flat chroma green #00ff00. On it, THREE cells,
each exactly 220x420: cell 1 at x=200..420, cell 2 at x=660..880,
cell 3 at x=1120..1340, all at y=300..720. No cell borders: only the
smoke against flat #00ff00.

Each cell: ONE thin column of pipe smoke rising from the cell's bottom
center, in the attached steam style: flat grey #8a8f98 chunks with
pale #c9c8c5 tops, each chunk at least 4x4 px, gentle S-curve, small
puffs separating near the top. No gradients, no noise, no
antialiasing, nothing green.

The three cells are one loop: the smoke pattern DRIFTS UPWARD one step
per cell: cell 2 is cell 1 risen ~16 px with the lowest puff reborn
small at the bottom; cell 3 rises again; cell 1 follows cell 3.
