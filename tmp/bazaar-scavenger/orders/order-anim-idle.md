# Codex order — scavenger idle sheet — 3 cells, sheet doctrine

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = the keeper sprite, frame f1, 264x338, the ONLY
identity authority. Never draw a border or a grid into the output.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1536x1024.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/anim-idle/gen-idle-sheet.png
then print GENERATED=<that path>. No post-processing.

Canvas 1536x1024, background flat chroma green #00ff00 everywhere
outside the three cells. ONE ROW of THREE cells, each exactly 264x338:
cell 1 at x=100..364, cell 2 at x=500..764, cell 3 at x=900..1164,
all at y=300..638. Each cell is a full repaint of Image 1 on its own
dark backdrop exactly as Image 1 has it.

Cell 1: an EXACT COPY of Image 1, unchanged.
Cell 2: same figure; ONLY the two gold eye slits move 6 px to his
left, and the smoke curls shift up one step. Nothing else changes.
Cell 3: same figure, eyes back to center; the pipe ember flares
brighter orange and one new small smoke puff appears at the bowl.
Nothing else changes.

Invariants for all cells: same colors, same size, same position
inside the cell, same pose, same pipe, same hood. Pixel-art flatness:
flat chunks, no gradients, no noise, no antialiasing. Only the listed
hexes of Image 1; the eyes stay gold, the ember stays ember orange.
