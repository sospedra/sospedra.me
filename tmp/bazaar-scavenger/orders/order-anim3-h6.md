# Codex order — scavenger hold frame — open hand, palm up

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = the keeper reference pose. Copy the figure, the
cloak, the hood void, the clasp, the colors. Change ONLY the gesture.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1024x1536.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/anim3-h6/gen-h6.png
then print GENERATED=<that path>. No post-processing.

The ENTIRE canvas is flat pure black #000000. ONE figure, centered,
about 900 px tall: the hooded keeper facing the viewer. His near arm
extends forward and down with an OPEN HAND, PALM UP, fingers spread
slightly: an offering gesture, presenting something below him. The
other hand holds the pipe down at his side. Eyes are two upturned
gold #dda23c CRESCENTS (a smile in the eyes). Silver leaf clasp
#c9c8c5 at the throat. Bandaged forearms #885323. Cloak #35402f with
soft lit folds. Flat pixel chunks, no gradients, no noise, no
antialiasing. Exactly two arms. Nothing else on the canvas.
