# Codex order — scavenger idle sheet v2 — keeper on void, 3 cells

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = the keeper reference. Copy ONLY the hooded figure,
his pipe, his smoke, his colors. Copy NOTHING else from it: no
doorframe, no wall, no counter, no wallet, no props.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1536x1024.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/anim2-idle/gen-idle-sheet.png
then print GENERATED=<that path>. No post-processing.

The ENTIRE canvas is flat pure black #000000. On it, THREE cells, each
exactly 192x335: cell 1 at x=250..442, cell 2 at x=650..842, cell 3 at
x=1050..1242, all at y=344..679. Nothing marks the cell borders: no
frames, no lines, only the figure placed identically in each cell.

Each cell: the seated keeper from Image 1, centered, same size in all
three cells, seated height about 240 px, pipe FULLY inside the cell.
Flat pixel-art chunks, no gradients, no noise, no antialiasing. He is
nearly black on black: cloak one flat dark olive #35402f, under-layer
#151719, bandaged forearms #482614, gloves #151719, hood void #030506.
Bright marks only: two gold #dda23c eye slits and the #e07830 pipe
ember. Grey #8a8f98 smoke curls, each chunk at least 4x4 px.

Cell 1: the rest pose: seated, pipe raised to the mouth, one thin
smoke wisp of three curls, eyes half-lidded slits.
Cell 2: IDENTICAL except the eye slits move 6 px to his left and the
smoke curls shift up one step.
Cell 3: IDENTICAL to cell 1 except the ember flares brighter, one new
small smoke puff at the bowl, eyes back to center.

Exactly two arms in every cell. Same silhouette in every cell: only
eyes, ember, and smoke change between cells.
