# Codex order — scavenger seated idle master — approved design, green hood

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = the APPROVED character design. Copy it exactly
with ONE change: the HOOD is now the same dark green as the cape
(#2b3526 body, #212819 shade, #35402f edge). Everything else keeps
its exact garment and color.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1024x1536.
Verify size; retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar-scavenger/jobs/anim8-idle/gen-idle.png
then print GENERATED=<that path>. No post-processing.

The ENTIRE canvas is flat chroma green #00ff00. ONE figure: the
approved keeper SEATED cross-legged, centered, exactly 786 px tall
from hood top to the lowest visible point, facing the viewer. One
hand raises a churchwarden pipe to the mouth: thin curved #963a23
stem, small bowl, #e07830 ember. The other hand rests on a knee. NO
smoke. Eyes: two amber half-lidded slits #f0a63c in the flat #030506
hood void.

THE PALETTE, use ONLY these colors: #030506 outline; greens #212819
#2b3526 #35402f (hood, scarf, cape, knee plates); darks #0d0e10
#111317 #16181d #1b1d22 (under-suit, rig, greaves, glove, sandal
straps); khaki #6e6148 #52483a (trousers); bone #c9c8c5 #8a8f98
(sandal plates, kit plate); #963a23 (plus mark, pipe); #e07830
(ember); #f0a63c (eyes). Merge strays into the nearest listed value.

FLATNESS: LOW DETAIL, large plain areas, every surface at most THREE
flat chunks, the cape at most four fold lines, zero gradients, zero
noise, zero antialiasing, blocks never smaller than 4x4 px, flat
lighting, no side light. Exactly two arms. Zero chroma green inside
the figure.
