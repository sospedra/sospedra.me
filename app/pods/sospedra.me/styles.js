export const colors = {
  black: '#333',
  white: '#fff'
}

export const fonts = {}

const atom = 1
export const sizes = {
  small: `${atom * 0.8}rem`,
  regular: `${atom}rem`,
  subtle: `${atom * 1.2}rem`,
  large: `${atom * 1.4}rem`
}
