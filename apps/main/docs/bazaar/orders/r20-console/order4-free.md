You are an image-generation subagent. Execute exactly this and nothing else.

Image 1 is the console stall COLOR AUTHORITY — sample every material
color from it; copy zero geometry. Image 2 is ED — identity
authority: same face, red dreads, skin, slight build; zero scene
geometry. Image 3 is a FRAME GUIDE: a dark frame on chroma — an
INVISIBLE CONSTRAINT. Never draw it, never echo it; the output has
NO frame. ALL art stays INSIDE where the frame sits.
Image 4 is the COMPOSITION BLUEPRINT: the drawn plan of this stall —
every dashed box is an element region, every label a contract. It is
an INVISIBLE CONSTRAINT: never draw its boxes, lines, labels or
colors; only obey its layout. The blueprint's proportions are the
truth the pixel boxes below restate.

GENERATE the redesigned CONSOLE stall on canvas 1536x1024, flat
chroma green #00ff00 everywhere except the stall.

WIDTH LAW — the redo reason, absolute: the stall is a TALL NARROW
den. Total art EXACTLY the frame width: 608 px wide (x=464..1072),
736 tall (y=184..920), ground at y=920. MEASURE the width before
returning: art wider than 620 px is a REJECTION. Racks are SLIM
towers, never wide cabinets.

ANGLE LAW — the bazaar axonometric with the camera slightly LOWER:
every vertical dead vertical; tops read as top bands at 1 px back
per 1.5 px across (a touch deeper than usual); NO vanishing points,
NO isometric rotation.

THE COMPOSITION — pixel boxes (each ±10 px):
- hanging sign "console" x=840..1048, y=197..270 — RIGHT-ALIGNED
  over the right rack: riveted plate held by a TWISTED, BROKEN scrap
  pipe pole — bent twice, one visible fracture with a hanging bolt,
  rust bites. The pole drops from the sign at x=992..1053,
  y=264..512 and DISAPPEARS BEHIND the right rack — it never reaches
  the ground visibly. The ONLY readable word. NO other pipes
  anywhere.
- LEFT rack: a slim SERVER TOWER x=477..667, y=520..894 (3 bands,
  amber/teal LED dot rows), ROTATED TOWARD ED: it shows its front
  face plus its INNER (right) side face as a parallel-projection
  reveal — the pair encloses him. ON ITS TOP: the ONLY LIT SCREEN in
  the stall — a CRT x=498..640, y=432..520, alive with flat two-tone
  static.
- RIGHT rack: a DIFFERENT construction and SHORTER: an OPEN SHELVING
  frame x=867..1059, y=584..894 (angle-iron uprights, open shelves
  with dark dead electronics and a coiled cable), ROTATED TOWARD ED:
  front face plus its INNER (left) side reveal.
- MONITOR WALL x=690..870, y=430..690: 2x2 stacked CRTs, ALL FOUR
  DARK dead screens with one diagonal glare band each.
- ED x=688..848, y=680..894: cross-legged on the rug, visor up,
  controller in lap.
- PERIPHERALS BOX at Ed's RIGHT x=851..979, y=808..898: an open
  cardboard box spilling two beige keyboards and a mouse by its
  cable, standing in front of the right rack's base.
- RUG, FULL STALL WIDTH x=464..1072, y=845..912: the camera sits
  lower so the rug band is DEEP — visible woven pattern rows
  foreshortened toward the back, fringe dots along the FRONT edge.
  Flat straight band, never a trapezoid.
- pizza box x=522..618, y=830..870.
- MULTI-OUTLET POWER STRIP (no keyboard anywhere): x=704..848,
  y=888..907 — a strip with four plugs in and one lit switch.
- cable spaghetti zone x=496..1040, y=780..900 — cables pool behind
  Ed only inside this zone.

LIGHT LAW — causal, single source: the ONE lit CRT throws one flat
cyan receiver band toward Ed and one small floor pool under the left
rack. Rack LEDs give thin amber spill strips only. Nothing else
emits.


FLOOR-PROP AXONOMETRIC LAW — absolute, the reason for this round:
objects LYING ON THE FLOOR (the rug, the pizza box, the keyboard,
the peripherals box footprint) obey the same parallel projection as
everything else:
- THE RUG is a FLAT STRAIGHT HORIZONTAL BAND: its left and right
  edges are VERTICAL, its top and bottom edges are HORIZONTAL and
  PARALLEL. Its pattern rows run horizontally, compressed vertically
  toward the top. It is NEVER a trapezoid, NEVER a diamond, NEVER
  converging, NEVER rotated. A rug with slanted side edges is a
  REJECTION.
- Box-shaped floor objects show their front face plus a shallow
  parallelogram top (1 px back per 1.5 px across), edges parallel.
- Image 4 draws the rug box: fill that box exactly, edge to edge.

STYLE — the bazaar doctrine: authored low-res 16-bit pixel art,
large flat bounded color regions, chunky square clusters, near-black
outlines, exactly three tones per material, sparse hard highlights,
LOW DETAIL.

Color palette: use ONLY these colors, nothing else:
#020307 #111923 #2b3741 #606970 #644d35 #7d6144 #96795a #dc7707
#f5b749 #7b7a78 #5a5a58 #9b9a98 #4c2815 #6e3d20 #8f5a2e #180802
#471907 #58240c #126e9b #1f9cc8 #4bd2e1 #ad6a1e #df9e32 #ffd26b

AVOID (hard rejections): antialiasing, gradients, blur, glow, bloom,
painterly shading, microtexture, noise, perspective convergence,
wall pipes, extra lit screens, extra readable text, watermarks, art
outside the frame, art touching the frame, characters other than Ed.

THE SOUL — creative freedom inside the boxes: Ed's den, a kid's
fortress of dead tech, comfortable like a nest. Invent small honest
details (abstract stickers, a folded blanket, worn tape marks) as
long as every element lands in its box, nothing is wider than the
frame, nothing new is bigger than a shoebox, and nothing else emits
light.

Call image_gen once to GENERATE at 1536x1024. Copy the result
unmodified to r20-console/gen4-free.png and print GENERATED=<path>.
