# r17 order — W98 — layered animation build (K-2SO gardener + creatures + water)

READ FIRST:
1. .../master-run-20260728/r17/DOCTRINE.md
2. The papers pilot reference implementations it names.
Work only inside .../master-run-20260728/r17/w98/

LOCKED STATIC: r15/gen-w98.png · Chroma #ff00ff.
(Fleet-10 render: the teal derelict robot watering, red-lamp wash.)

## The stall, described

A greenhouse stall. THE ROBOT stands center: tall, lanky, K-2SO
silhouette, TEAL body with rust patches, domed head with two pale
round eyes, canvas APRON, holding a WATERING CAN mid-pour — a thin
WATER ARC with droplets falls into a terracotta pot with a sprout.
Left: a red-board plant rack under a RED GROW LAMP (everything below
it wears a red wash), pink/red/white flowers, strawberries, a blue
FUZZY CREATURE with yellow eyes and antennae at the bottom. Right: a
plant tower with tomatoes and peppers, a SLEEPING blue OCTO creature
on top, a veg crate below. A bulb string crosses the top; a "w98"
plank sign hangs left.

## Extraction duties

- CHARACTER = THE ROBOT: dome, body, apron, both arms, legs, the
  watering can in his grip. NOT the water arc (separate layer). Probe
  the teal cluster (teal #008080-family + rust browns + apron
  canvas).
- WATER = EFFECT-PROP LAYER (fx-water): the arc + droplets between
  can spout and pot. Small cluster of pale blue pixels — probe it.
  Inpaint behind it: trivial (dark background), nearest-side.
- OCTO = EFFECT LAYER (fx-octo): the sleeping blue octopus on the
  tower top, incl. its pink tail tip. Probe its blue cluster.
- FUZZY = EFFECT LAYER (fx-fuzzy): the blue fuzzy creature bottom-
  left, incl. antennae and its yellow eyes.
- PROTECTED: every plant, both racks, the lamp + its red wash, the
  sign, bulb string, pots, veg crate, the sprout pot he waters,
  chroma.
- Inpaint behind the robot: the arch/wall gap behind him (dark
  foliage/wall) — strip-tile from clean columns; floor rows
  nearest-side. Behind the creatures: rack boards / tower shelf —
  strip continuation.

## Idle — 3 frames (rest 1800ms, A 200ms, B 200ms)

- f2 (A): the POUR LIVES: water arc frame A (droplets at positions
  1/3 along the fall), robot's dome tips 2px toward the pot, his
  LEFT eye dims one palette step (alternating blink).
- f3 (B): water arc frame B (droplets at positions 2/4, one droplet
  splashing at the soil — 2px wider splash pixels from the water's
  own colors), dome back, RIGHT eye dims instead, one tomato-vine
  leaf beside him sways 3px (leaf pixels shear).
  (The water arc frames live in fx-water so they can also run as an
  infinite loop — see Effects. In idle the char frames only carry
  the dome/eye motion.)

## Hover — 4 frames (150ms each, h4 held)

- h1: the dome TURNS to camera: both pale eyes front, dome outline
  shifts 8px viewer-ward (rebuild the dome band from its own teal
  shades), eyes brighten one step.
- h2: the POUR STOPS: fx-water swaps to its EMPTY frame (falling
  remnant droplets only — author fx-water-stop.png with 3 loose
  droplets mid-air); the can arm LOWERS 12px (rigid region lift
  down, elbow root fixed).
- h3: the free gripper GESTURES at the sprout pot: free arm rebuilt
  rotated outward, gripper travels ~25px toward the pot, fingers
  open.
- h4 (held): dome tips 3px (friendly), gripper holds the gesture,
  can arm still. Held. (Water stays stopped while hovered.)

## Effects

- fx-water: 3 frames, 240ms, infinite while idle: arc with droplets
  at phase 1, phase 2, phase 3 (droplets progress down the arc; the
  arc line itself constant). Plus fx-water-stop.png used during
  hover h2..h4.
- fx-octo: 3 frames, 1100ms full breath: body swells 3px taller
  (top rows duplicated) and settles 1px lower; tentacle tip curls
  2px on the settle frame. Slow, sleeping.
- fx-fuzzy: 3 frames, 700ms: double-blink (yellow eyes covered by
  its own blue fur shades for 1 frame), antennae sway 4px
  (tips shear left then right).

## Deliver

Doctrine output contract in r17/w98/, manifest.json (idle 3, hover
4, fx-water 3 @240 + stop frame, fx-octo 3 @1100, fx-fuzzy 3 @700),
verify.mjs green, DONE line.
