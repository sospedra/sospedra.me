# Codex subagent order — Bazaar 3 Floor 1 master (round 5: small corrections)

You are an image-generation subagent. Execute exactly this task and nothing else.

The attached images are, in order:

- Image 1: EDIT TARGET — the round-4 Floor 1 master. Its rendering style is the floor-wide canon. Everything is approved direction except the corrections below.
- Image 2: geometry guide v2 (bay boundaries for reference).

Steps:

1. Call your built-in image_gen tool ONCE in EDIT mode with Image 1 as the edit
   target, at size 1536×1024 (wide landscape), using the EDIT SPEC below.
2. Verify the output PNG is exactly 1536×1024. If not, retry (max 2 attempts).
3. Copy the final PNG unmodified to exactly:
   /Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/candidates/floor-1-r5.png
4. Print one line: GENERATED=<that absolute path>
5. No post-processing. No repository edits. No other commands.

## EDIT SPEC

Use case: precise-object-edit. Keep the ENTIRE scene exactly as in Image 1 —
composition, camera, stalls, characters, beams, stairs, lobby, lighting,
palette, the chunky flat pixel rendering — except these three corrections:

1. Ramen bowl: place a ceramic ramen bowl with a small flat steam wisp on the
   countertop directly in front of the seated customer. Partial occlusion by
   the customer's back is fine; a visible rim and steam are enough.
2. Papers fills its bay: extend the Papers kiosk structure and its shelving
   sideways so the stall spans the full width between its two flanking
   H-beams, with no dead empty wall strip left inside the bay. Keep the same
   teal kiosk design, scalloped awning, papers lettering, hologram archivist,
   book, racks. The awning and side shelving grow; nothing else changes.
3. Katakana cleanup: the small worn row under the "uses" sign reads exactly
   ラーメン, small and legible, same worn style.

Everything else is immutable: same beams (pairs intact), same complete right
stair, same chef, customer, stools, crates, lanterns, hanging bulb, ember
glow, hologram receivers, rear-darker-than-front lighting, flat #020307
matte, no arrows, no drains, no new text.
