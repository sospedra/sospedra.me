# Codex order — r16 — Talks — hover frame 2 (frame 4/5)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached:
- Image 1 = the generated Talks hover frame 1 and THE EDIT TARGET. It
  is the authority for every pixel outside THE MOTION below.
- Image 2 = the Talks static master (idle frame 1): identity and
  resting-pose reference.

Steps: call image_gen ONCE in EDIT mode with Image 1 as the edit
target, output size 1536×1024. Verify size; retry once if wrong. Copy
the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r16/gen-talks-hover2.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## FRAME LAW — absolute

This is frame 4 of 5 of an in-place animation (second hover frame). At
runtime the frames swap on the same CSS box. Image 1 is law for every
pixel outside the declared motion.

- Canvas 1536×1024, flat chroma green #00ff00 background, art exactly
  where Image 1 has it. No translation, no rescale, no crop change.
- Reproduce Image 1 pixel-for-pixel everywhere except THE MOTION:
  same neon sign, kiosk, tape walls, posters, CRT, cart, standee,
  counter and every prop, outlines, colors, lighting and glow shapes.
- The clerk's SEAT is the fixed root: she articulates upright around
  it. The torso rises by rotation/straightening, it never translates
  left or right. Nothing rescales.
- No lighting or brightness change anywhere.
- ZERO new colors: every pixel value must already occur in Image 1.
- The CRT's SMPTE bars and the neon glow stay byte-frozen.
- CRITICAL: NO shelf, rack, bin, cart or box pixel changes. The tape
  she picks comes from UNDER the counter, invisible storage. Both
  tape walls keep every spine exactly as Image 1.

## THE MOTION — the only change

She sits up and selects a tape:

1. Her head lifts off the palm: neck straight, head upright at the
   same x. Where her head/hair no longer cover the tape wall, the
   revealed wall must CONTINUE Image 1's exact tape pattern.
2. The propping arm un-plants: that hand drops to rest flat on the
   counter top.
3. Her other hand rises above the counter line holding ONE VHS tape,
   brought up from below the counter. Draw the tape with hexes
   already on the tape walls (dark shell + one colored label band).
   She holds it at chest height, eyes reading its label.
4. Her gaze aims at the tape label this frame, not the camera.

The changed region is confined to her head/torso/arms and the new
tape, all inside the kiosk interior between counter top and hanging
lamp. She must not overlap the CRT, the box, the bell or the display
stand.

## FREEZE CHECK — explicitly unchanged

"VIDEO CLUB" neon + mounts, kiosk frame, both tape walls and every
spine, posters, price board, vinyls, boombox, ornaments, hanging
lamp, CRT + antennas + SMPTE bars, tape cart + every tape in it,
decals, rewind badge, bell, card box, display stand, the standee,
counter body and its taped patches, chroma field.

## SELF-CHECK before returning

1) Difference vs Image 1 is confined to the clerk + the one new tape.
2) Both tape walls and the cart match Image 1 spine-for-spine; the
   tape wall behind her old head position continues the pattern.
3) Seat root fixed; torso upright by articulation, x unchanged.
4) No new hexes; no lighting or glow change anywhere.
5) 1536×1024; clean #00ff00 chroma field.
