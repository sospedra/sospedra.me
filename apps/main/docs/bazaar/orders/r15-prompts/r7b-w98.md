# Codex order — r7 strategy B — Floor 3 w98 bay crop (v2 canvas)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = EDIT TARGET, a 1536×1024 padded crop from the Floor 3
master. Black border = matte, keep it flat. Only the content inside the paste
window x560–960, y213–810 will be kept; everything outside it will be
DISCARDED at paste time — do not spend effort preserving it, only keep the
seam edges consistent where they meet the window.

Steps: call image_gen ONCE in EDIT mode, size 1536×1024 (wide landscape),
spec below. Verify the output is exactly 1536×1024; retry once if wrong.
Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r7/gen-w98.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## EDIT SPEC

Keep the w98 garden stall exactly as it is — yellow-ochre robot watering the
seedling, w98 sign, foliage, pots, creatures, string bulbs, purple lamp — in
the same chunky flat pixel style (3×3 blocks, three tones per material,
near-black outlines). Apply ONLY this lighting, exact geometry, hard flat
palette steps:

1. The purple lamp (near (785,375)) visibly lights its surroundings:
   - the robot's BACK plates (the side toward the lamp): one violet step over
     rect (815,450 45×170);
   - the seed trays and leaves below the lamp: rect (665,560 110×60);
   - the lamp keeps a small violet core and a compact contact below.
2. The background bulb string (around (765,310)) puts faint warm dots on the
   foliage tops: rect (655,340 280×40).
3. Rear plane of the stall one step darker than the front; the tiny
   dusty-rose creature glows stay as they are.

Nothing moves, nothing is added or removed, no text changes ("w98" stays).
No aberrations: robot lenses, sign glyphs, creature shapes stay crisp. Flat
steps only, no glow, no gradients.
