# r17h order — MANUAL hover rebuild (translation law)

READ FIRST: r17/DOCTRINE.md, then r17/HOVER-DOCTRINE.md, then
r17/author-hover.mjs (papers pilot). Work only in r17/manual/.
Rebuild ONLY char-h1..h4.png + verify envelopes. Idle, plate,
manifest structure: UNTOUCHED.

The old hover synthesized a rotated presenting claw — REJECTED. New
read: lock on → curious dip → eager rise with the duster lifted →
settled friendly float.

## Groups to probe first (debug crops mandatory)

- ROBOT: the whole char layer (already isolated).
- DUSTERARM: the duster + its arm cluster — BFS from the duster
  feathers inward; stop the group INSIDE the body shell so the
  trailing boundary overlaps same-material olive plating when it
  moves.
- PUPILS: the three eye dots inside the stalk eyes.

## Frames (150ms each, h4 held)

- h1 LOCK: all three PUPILS translate to camera-center; the eye
  whites' brightest pixels swap one palette step brighter (existing
  colors only).
- h2 DIP: the ENTIRE layer translates down 12 — the robot drops
  toward the viewer curiously. Flame overlaps the bench top rows; it
  is bright and in front, correct.
- h3 RISE: the ENTIRE layer translates up 28 from h2 (net +16 above
  rest — an excited hop) AND DUSTERARM translates up a further 8
  (raised in greeting, trailing edge overlaps the shoulder shell).
- h4 SETTLE: layer translates down 10 from h3 (net +6 above rest,
  floating attentive), DUSTERARM stays raised, pupils stay locked.
  Held.

## Verify

Envelopes: whole-layer moves = full char bbox ±30. Green before
DONE manual-hover VERIFY=PASS.
