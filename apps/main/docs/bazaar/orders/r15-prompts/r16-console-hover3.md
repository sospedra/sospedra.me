# Codex order — r16 — Console — hover frame 3, held (frame 5/5)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached:
- Image 1 = the generated Console hover frame 2 and THE EDIT TARGET.
  It is the authority for every pixel outside THE MOTION below.
- Image 2 = the Console static master (idle frame 1): identity and
  resting-pose reference.

Steps: call image_gen ONCE in EDIT mode with Image 1 as the edit
target, output size 1536×1024. Verify size; retry once if wrong. Copy
the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r16/gen-console-hover3.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## FRAME LAW — absolute

This is frame 5 of 5 — the HELD hover frame: it stays on screen while
hover/focus remains, so it must read casual and stable. Image 1 is law
for every pixel outside the declared motion.

- Canvas 1536×1024, flat chroma green #00ff00 background, art exactly
  where Image 1 has it. No translation, no rescale, no crop change.
- Reproduce Image 1 pixel-for-pixel everywhere except THE MOTION:
  same racks, servers, CRT, sign + bent pipe, boxes, cables, rug,
  pizza, power strip, outlines, colors, lighting and glow shapes.
- Ed's seated root is fixed: pelvis and crossed legs keep Image 1's
  exact position on the rug. The torso never translates left or
  right. Nothing rescales.
- The visor STAYS ON in every frame.
- No lighting or brightness change anywhere.
- ZERO new colors: every pixel value must already occur in Image 1.
- CRT static pattern, every rack LED dot, and all cable runs stay
  byte-frozen.

## THE MOTION — the only change

The peace sign holds; the other hand goes back to work:

1. The raised viewer-right peace sign stays EXACTLY as Image 1 —
   copy it pixel for pixel. Casual, effortless.
2. The viewer-left hand returns to Image 2's resting pose over the
   leg, fingers settled back to tracking the invisible interface.
3. The head keeps facing the camera through the visor; the mouth may
   relax 1 px into a faint acknowledgment.
4. Everything else: byte-identical to Image 1.

The changed region is confined to the viewer-left hand (+ ≤1 px
mouth).

## FREEZE CHECK — explicitly unchanged

"console" sign + bent pipe + chain, CRT + its static, speaker box,
every rack unit and LED, stacked cartons, keyboard/headphone box,
cable spool, power strip + plugs, pizza box + slice, rug + fringe,
Ed's head angle, the raised peace-sign arm, torso and legs, chroma
field.

## SELF-CHECK before returning

1) Difference vs Image 1 is confined to the left hand (+ ≤1 px
   mouth).
2) Peace-sign arm matches Image 1 to the pixel; left hand matches
   Image 2's resting pose.
3) Seated root and torso x unchanged; visor on.
4) No new hexes; no lighting or glow-shape change.
5) 1536×1024; clean #00ff00 chroma field.
