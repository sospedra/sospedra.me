# Codex order — r16L — Papers — idle motion frame (loose, frame 2 of 2)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = THE EDIT TARGET — the locked Papers stall static.

Steps: call image_gen ONCE in EDIT mode on Image 1, output size
1536×1024. Verify size; retry once if wrong. Copy the result
unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r16/gen-papers-idle2-loose.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## THE TASK

This is frame 2 of a TWO-FRAME idle loop. Frame 1 is Image 1,
unchanged. You are drawing the other half of the loop: the cyan
HOLOGRAM ARCHIVIST's small idle motion. YOU choose the motion — a
natural, lively hologram idle (signal shimmer, a slow blink, a slight
head turn, a breathing lean, drifting glitch fragments — your call).
Make it CLEARLY VISIBLE when the two frames alternate: a viewer
watching the loop must notice the hologram is alive. Keep it an IDLE:
he keeps reading at his counter, no pose change, no new objects.

## THE FREEZE LAW — absolute

Everything that is not the cyan hologram man is FROZEN, pixel-for-
pixel identical to Image 1: the awning, the "papers" sign, the
shelves and every book spine on them, the counter, the brass bell,
the pen cup, the closed book on the counter, the card racks, the
page boards, the tan open book IN HIS HANDS, the floor, and the flat
magenta chroma field. Same canvas, same crop, same scale, same
placement — the two frames swap in place on the same CSS box, so ANY
changed pixel outside the hologram will flicker as an error.

Do NOT restage the composition. Do NOT recolor, sharpen, smooth or
"improve" anything. Do NOT move the camera. This is a pixel edit of
Image 1, not a new rendering of the same stall.

## SELF-CHECK before returning

1) Overlaying Image 1: the ONLY differing pixels belong to the cyan
   hologram (and his glitch fragments).
2) The tan book in his hands is byte-identical. The bell, sign,
   shelves, counter: byte-identical.
3) The motion is visible at a glance when frames alternate.
4) Flat #ff00ff chroma outside the art, clean edges.
5) 1536×1024.
