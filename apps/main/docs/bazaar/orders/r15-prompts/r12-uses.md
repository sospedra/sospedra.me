# Codex order — r12 — Uses asset: add the marquee top

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = EDIT TARGET, the Uses stall asset on flat chroma green
#00ff00, 1536×1024. Everything is APPROVED except the missing marquee top.

Steps: call image_gen ONCE in EDIT mode with Image 1 as the edit target, size
1536×1024. Verify size; retry once if wrong. Copy the result unmodified to
exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r10/gen-uses-r12.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## EDIT SPEC

ONE addition. The canopy currently ends at the top of its striped band and
the two sign ropes rise into nothing. Add the marquee's top structure:

1. A horizontal wooden ridge board running the FULL width of the canopy,
   sitting directly on top of the striped band. It is 14 to 18 pixels tall.
   Colors, exactly these three: body #4b2816, bottom shadow row #321a0f, top
   light row #6b391c. Hard horizontal edges, near-black #020307 outline.
2. Two rope anchor knots where the sign ropes meet that ridge: each rope
   (color #925022 with shadow side #6b391c) rises from a top corner of the
   "uses" sign board and ties over the ridge with a small knot block of
   #6b391c with a #321a0f shadow row. The ropes end AT the ridge — nothing
   rises above it.
3. The ridge casts one 2-pixel shadow row of #321a0f onto the striped band
   directly beneath it.

Nothing else changes: the striped band, the sign and its ラーメン row, the
lanterns, the bulb, the chef, the customer, the counter, stools, crates,
platform — all stay pixel-identical. Style stays chunky flat pixel art on a
3×3 grid, three tones per material, near-black outlines, no gradients, no
noise. Background stays perfectly flat #00ff00 with nothing touching the
canvas border. Use ONLY the hex colors named above for the new pixels.
