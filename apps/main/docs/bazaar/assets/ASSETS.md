# assets manifest

## composed (built for the blog, compose.mjs regenerates)

- composed-console-convergence.png — the angle war in four panels:
  gen7 (62deg) -> gen8b (corrective, 55.5) -> gen10-p2 (repaint lock)
  -> gen11-a (portrait, shipped).
- composed-stall-war-30.png — the 30 rejected stall-war masters. The
  cold-open image: every one obeyed the same poisoned order.
- composed-ed-war-30.png — the 30 Ed camera strategies of r21.
- composed-stall-lineup.png — the eight shipped stalls at family
  display scale, bottom-aligned.
- composed-prop-inventory.png — all 92 decoration sprites, one grid.
- composed-proto-a1900.png / -b1000.png / -m650.png — the layout proto
  with live gap rulers in regimes A, B, and mobile.

## instruments and gospels (r20 console war)

- angle-law.png — the original drawn law: green legal construction,
  red crossed-out isometric (r12, attached to every scene order since).
- slab-angles.png — the user's slab diagram: 130/50/50/130, 68:81.
- angle-verdict.png — right-vs-wrong slab card with the previous
  failure's measured numbers.
- corner-vector-crop.png — the user's first corner SVG (23:27 draft).
- corner-w98.png, counter-papers.png — painted corner-law proofs cut
  from shipped stalls.
- composition7/8/9.png — the drawn plans: slab vertices, checkpoint
  dots, element boxes, doctrine panels (7 = slab debut, 8 = nest +
  checkpoints, 9 = verticality + 15-color law).
- blueprint.png, frame-guide.png — the pre-slab blueprint era.
- games-composed.png — the world-camera gospel (games stall from
  shipped layers).
- bebop-ed.png — Ed's character gospel (the film still).
- portrait-guide-a.html — a portrait plan guide as code.

## the console lineage (full frames)

- gen7-master.png, gen8b.png, gen10-p2.png, gen11-a.png, gen11-b.png
  — the strip's sources plus the rejected wider portrait probe (11-b,
  family-exact width but a 5x plinth).

## rescue/ (pulled from disposable locations)

- floors/ — the original whole-floor masters, rounds r1-r8. The
  campaign's "before".
- r15/ — proportion guides (guide-*.png, the drawn-law instrument),
  console-lineage.png, carpet-law*.png, compare-5v6.png, ab-*.png A/B
  strips, composition-shot.png.
- r16-diffs/ — red-overlay drift diffs: why whole-frame edits died.
- r19-sheets/ — the decoration sheet gens, pre-slice (46 sheets across
  four fleet rounds).
- rulers/ — measure-games.png pixel ruler + floor geometry guides.
- layout-proto/ — bz4-layout-proto.html (the validated layout system,
  self-contained).
- sm-gate/ — SM candidates b, c, b2 (the winner) + the gate pages.

Shipped assets are NOT duplicated here; they live in
public/images/bazaar4/ (hand-polish law applies there).

## harness-shots/ (the method's own UI, captured live)

- gate-index / gate-r22 / gate-r23 / gate-r24 — the SM stairs gate
  pages: candidate probes side by side with measured aspect + deck
  lines and LAW PASS verdicts. How rulings happened.
- gallery-gen9-wars — the r20.9/r20.10 war board: six tests, probes,
  references, verdict captions.
- gallery-orders-viewer — the 30 stall-war order texts viewer (the
  audit that convicted the shared prompt body).
- gallery-stall-war / gallery-ed-war — the raw 30-gen war boards as
  the user judged them.
- gallery-console-candidates — rounds 1-3 A/B/C console candidates.
- street-compare — the r18 street kit compare harness (composed
  stages + animation lab + per-asset grid).
- deco-inspect — the r19 decoration inspector.
- anim-preview-all — the r17 eight-stall animation preview runtime.
