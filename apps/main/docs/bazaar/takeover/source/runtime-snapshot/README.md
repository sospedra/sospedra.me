# Rejected runtime snapshot

This directory is a copy of the Bazaar 3 runtime source as it existed on
2026-07-28.

It is included so the next AI can diagnose, compare, and preserve solved
interaction/responsive behavior without searching the repository.

It is **not** an approved visual foundation.

The copied floor systems use generic panels, repeated H-beams, CSS receivers,
and complete stall images. The user rejected the resulting integration.

Canonical live files remain under `app/bazaar3/`. Do not edit this snapshot and
expect the application to change.

