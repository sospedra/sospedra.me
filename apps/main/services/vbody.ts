// the shell content root: hotkeys resolve scroll surfaces from this id.
// the document itself scrolls; this div only anchors the search.
// leaf module: shell and hotkeys both need it and must not import each other
export const VBODY_ID = 'vbody'
