# CIMS plan

Spec: `docs/superpowers/specs/2026-08-05-cims-design.md`. Every task ends green on `pnpm typecheck` for the touched files. Paths are relative to `apps/main`.

## 1. Data and dependency

1. Extract line 158 of the prototype into `public/cims/terrain.json` (strip `const DATA = ` and the trailing `;`). Verify with `node -e "JSON.parse(...)"` and key listing.
2. `pnpm add three@0.185.1` and `pnpm add -D @types/three@0.185.4` in `apps/main`. Check `git diff` keeps the `packageManager` stamp intact.

## 2. Golden vectors

3. Write a scratch script (`/tmp`, not committed) that extracts `decodeMasked`, `smoothGrid`, `rampCol`+helpers, `sunPos`, `moonPos`, `smoother`, `sstep`, `wrapPI`, `lineB`, `contB` from the prototype and dumps `app/cims/golden-vectors.json`: synthetic b64 decode cases, one real 16×16 slice, ramp samples, three astronomy instants, easing samples.

## 3. Pure core (test-first per module)

4. `terrain-schema.ts`: zod envelope + `TerrainData` types.
5. `decode.ts` + `decode.test.ts`.
6. `ramps.ts` + `ramps.test.ts` (rgb tuples, no three).
7. `astronomy.ts` + `astronomy.test.ts`.
8. `flight.ts` + `flight.test.ts` (state union, damping step, launch geometry, arrive frame).
9. `ground-hit.ts` + `ground-hit.test.ts` (injected sampler).
10. `terrain-build.ts` + `terrain-build.test.ts` (base mesh, grid segs, contour segs, border segs, river segs as arrays).
11. `patch-slots.ts` (+ coverage inside `terrain-build.test.ts` if shared shapes).
12. Add the new suites to the `test` script `node --test` list.

## 4. Three shell

13. `shaders.ts`: terrain, river, edge GLSL + uniform factories.
14. `scene.ts`: renderer (color management off), fog, world group, terrain materials, base meshes from builders, depth-edge pass plumbing.
15. `sky.ts`: glow discs, sun/moon point pairs, orbit rings.
16. `trail.ts`: route, tail, head, `buildTrail`/`updateTrail`.
17. `sfx.ts`: blips, travel/arrive motifs, flight drone, resume-on-input.

## 5. Engine and input

18. `cims-store.ts`: snapshot type + store factory.
19. `engine.ts`: init (slot 0, drop-in), frame loop as named steps (flight step, orbit step, fog/near, sky, labels, HUD cadence, compass, three-pass render), store publishing, full dispose.
20. `pointer-input.ts`: pointer sessions (orbit/pan/look/pinch), tap and double-tap, wheel-anchored zoom, keyboard with interactive-target guard and canvas-focused orbit keys.

## 6. React view

21. `cims.module.css` + component modules: stage, scanlines, vignette, HUD, panel, labels, compass (port prototype CSS, camelCase classes, mobile rules).
22. `stage-labels.tsx`: peak/city/dest/sun/moon label pools with refs the engine writes.
23. `telemetry-hud.tsx`, `tour-panel.tsx` (buttons, dots, caption typewriter, slider), `compass.tsx`.
24. `cims-view.tsx`: fetch + validate, load states, engine mount effect, quiet wiring, status region.
25. `page.tsx`: metadata, server shell.

## 7. Gates and verification

26. `pnpm typecheck && pnpm lint && pnpm test`.
27. Dev server + headless Chrome: screenshot `/cims` and the prototype at 1440×900; compare boot, arrival, contour, points frames. CDP keyboard pass.
28. Hostile pass: fx-quiet, keyboard-only, error state (block the JSON route).

## 8. Close

29. Commits in order: docs, data+deps, core+tests, shell+engine+view. Scoped paths only (concurrent writer doctrine).
30. Memory note for the port.
