# Codex order — r8 — extract the Papers stall (retry: exact lettering)

Repeat the previous Papers extraction task exactly, with ONE hard correction.

Attached: Image 1 = the Papers stall crop from the Floor 1 master.

Steps: call image_gen ONCE to GENERATE a NEW image at size 1536×1024, spec
below. Verify size; retry once if wrong. Copy the result unmodified to
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r8/gen-stall-papers-v2.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## IMAGE SPEC

Same as before: redo the Papers archive kiosk from Image 1 as ONE isolated
asset on a perfectly flat solid #ff00ff background. Exact same layout,
structure, silhouette and pose as Image 1: teal kiosk, scalloped cream/teal
awning, interior shelves with books and paper bundles washed slightly blue,
hologram archivist rising from a hidden floor projector (glasses, physical
open book, hard cyan scanline steps, no lower body below the counter, no
projector pad), warm shelf strip, A-frame pamphlet rack, wheeled display
rack, kiosk base. Flatter colors, three tones per material, strong near-black
outlines, chunky 16-bit shading on a strict 3×3 grid, zero noise, zero
antialiasing, zero gradients. No H-beams, no wall, no floor, complete
silhouette, generous padding.

HARD CORRECTION — the awning lettering spells exactly:  p a p e r s
Six letters. One single letter s at the end. Lowercase. Nothing else readable
anywhere in the image. Verify the spelling letter by letter before returning.

Background #ff00ff perfectly flat, no spill, no shadows, and #ff00ff appears
nowhere in the art.
