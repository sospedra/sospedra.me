# Codex order — r16 — Travel — hover frame 3, held (frame 5/5)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached:
- Image 1 = the generated Travel hover frame 2 and THE EDIT TARGET.
  It is the authority for every pixel outside THE MOTION below.
- Image 2 = the Travel static master (idle frame 1): identity and
  resting-pose reference.

Steps: call image_gen ONCE in EDIT mode with Image 1 as the edit
target, output size 1536×1024. Verify size; retry once if wrong. Copy
the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r16/gen-travel-hover3.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## FRAME LAW — absolute

This is frame 5 of 5 — the HELD hover frame: it stays on screen while
hover/focus remains, so it must read proud and stable. Image 1 is law
for every pixel outside the declared motion.

- Canvas 1536×1024, flat chroma magenta #ff00ff background, art exactly
  where Image 1 has it. No translation, no rescale, no crop change.
- Reproduce Image 1 pixel-for-pixel everywhere except THE MOTION:
  same triangle sign, booth frame, corrugated walls, LAST SEATS
  plate, planet route cards, diving helmet, banjo, candles, radar
  unit, counter, chest, queue posts + rope, outlines, colors,
  lighting and glow shapes.
- The Hearthian's torso column at the counter is fixed. The torso
  never translates left or right. Nothing rescales.
- No lighting or brightness change anywhere.
- ZERO new colors: every pixel value must already occur in Image 1.
- Candle flames and the radar's green sweep stay byte-frozen.

## THE MOTION — the only change

The ticket stays proudly up; the other hand sells the route:

1. The raised ticket arm holds EXACTLY Image 1's pose — copy it
   pixel for pixel.
2. The OTHER gloved hand lifts off the counter and points diagonally
   up toward the three planet route cards on the rear wall: index
   finger extended, fingertip stopping at least 8 px from the nearest
   card. The forearm may pass in front of his vest and occlude it;
   where the vest stays visible it matches Image 1. Where the hand
   leaves the counter, the counter top must CONTINUE Image 2's exact
   wood pattern.
3. The four eyes stay wide on the camera; the smile gains 1 px of
   proud showmanship.

The changed region is confined to the pointing arm/hand, the counter
patch it vacated, and ≤1 px of mouth.

## FREEZE CHECK — explicitly unchanged

Triangle sign + saucer art, booth frame + rivets, corrugated walls,
star-map rear wall, planet route cards, LAST SEATS plate, diving
helmet, banjo, candles + flames, radar + sweep, counter (pattern
continued where the hand left), chest, queue posts + rope, the
raised ticket arm + ticket, his torso and head position, chroma
field.

## SELF-CHECK before returning

1) Difference vs Image 1 is confined to the pointing arm, the
   vacated counter patch and ≤1 px of mouth.
2) Ticket arm matches Image 1 to the pixel; fingertip ≥8 px from the
   cards.
3) Torso column unchanged.
4) No new hexes; no lighting or glow change anywhere.
5) 1536×1024; clean #ff00ff chroma field.
