# Codex order — r7 strategy A — Floor 1 whole-floor consolidated edit

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = EDIT TARGET, the Floor 1 master (1536×1024, black matte
border stays perfectly flat).

Steps: call image_gen ONCE in EDIT mode, size 1536×1024, spec below. Verify
size, retry once if wrong. Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/candidates/floor-1-r7a.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## EDIT SPEC

One consolidated pass. Keep the whole scene — composition, camera, beams,
stairs, chef, customer, lobby — in its exact chunky flat pixel style, and
apply ONLY the changes below. Regions not named must stay identical; do not
introduce render aberrations anywhere: faces, hands and sign glyphs stay
crisp.

1. Katakana: the row under "uses" reads exactly ラーメン. No bowl is added
   anywhere on the counter.
2. Uses lantern light, exact geometry (canvas coordinates), hard flat
   one-step warm receivers: canopy underside bands at (140,380 240×25) and
   (420,380 220×25); chef near side (350,410 90×90); counter top ends
   (130,555 200×25) and (450,555 190×25); left beam inner face faint
   (106,300 16×120). Lanterns keep tiny cores and compact contacts. The
   hanging bulb near (300,270) becomes decorative: tiny core, no pool.
3. Papers bay rebuild (previous pass aberrated it): the teal kiosk and its
   shelving span the full bay width between its beams (x840–1311) — wider
   scalloped awning with "papers" lettering, side shelving to both beam
   edges, complete pamphlet and wheeled racks. The hologram archivist rises
   from a HIDDEN FLOOR PROJECTOR: visible torso, book and head from the
   counter line (y≈560) up, no lower body, NO projector pad on the counter;
   glasses, composed, physical open book, hard cyan scanline steps.
4. Hologram interior wash, hard flat steps: interior shelves and books one
   blue step (920,380 370×170); awning underside (900,335 400×25); counter
   top band (900,560 400×25); both flanking beam inner faces faint
   (824,400 16×160) and (1311,400 16×160).
5. Every stall's rear plane one step darker than its front. Lobby stays dark.

Text: only "uses" + ラーメン and "papers". No arrows, no drains, flat matte,
flat palette steps, no glow, no gradients.
