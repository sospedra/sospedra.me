# Codex order — r7 strategy B — Floor 3 Travel bay crop (v2 canvas)

You are an image-generation subagent. Execute exactly this and nothing else.

Attached: Image 1 = EDIT TARGET, a 1536×1024 padded crop from the Floor 3
master. Black border = matte, keep it flat. Only the content inside the paste
window x582–955, y213–810 will be kept; everything outside it will be
DISCARDED at paste time — do not spend effort preserving it, only keep the
seam edges consistent where they meet the window.

Steps: call image_gen ONCE in EDIT mode, size 1536×1024 (wide landscape),
spec below. Verify the output is exactly 1536×1024; retry once if wrong.
Copy the result unmodified to exactly
/Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r7/gen-travel.png
then print GENERATED=<that path>. No post-processing, no repo edits.

## EDIT SPEC

Keep the Travel booth design — deep dark rear wall, travel sign, LAST SEATS
board, route cards, suit and helmet, radar with its green sweep as the only
counter object, luggage and queue posts — in the same chunky flat pixel
style. Apply ONLY:

1. Candles replace lanterns: REMOVE both hanging amber lanterns. Add one
   cluster of three stubby candles on a small dish at the counter's left end
   (near (631,585)) and two candles on the right shelf (near (901,560)).
   Tiny warm flame cores, hard flat steps, compact contacts.
2. Candle light, exact geometry, one warm step each:
   - the agent's front (jacket, scarf, near hand): rect (681,470 160×120);
   - the counter top: rect (621,590 320×20);
   - the LAST SEATS board edge, faint: rect (626,430 60×45);
   - the right return inner face and shelf: rect (871,500 80×80).
3. The Hearthian steps slightly deeper into the booth: his silhouette shifts
   back so the counter occludes him up to mid-chest and his head top drops
   about 15px; keep his exact design (blue-grey skin, four amber eyes in two
   pairs, pointed ears, scarf, jacket, one hand on the counter, one raising
   the ticket) and his corrected adult proportions.
4. The radar's green scope glow stays on the counter and his glove.
5. Strong value contrast stays: rear wall two steps darker than counter and
   agent.

Only "travel" and "LAST SEATS" are readable. No aberrations: eyes, ears,
glove, sign glyphs stay crisp. Flat steps only, no glow, no gradients.
