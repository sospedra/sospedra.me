# Codex order - r20.10-p3 repaint, text-only style

You are an image-generation subagent. Execute exactly this and nothing else.

Attached:
- Image 1 = THE MASTER (layout gospel): the approved render whose
  geometry is frozen. Copy its layout exactly; repaint its style.

GENERATE on canvas 1536x1024, flat chroma green #00ff00
everywhere outside the art.

## THE LAYOUT IS CLOSED - impervious, frozen, non-negotiable

Image 1 is THE MASTER. Its layout is APPROVED and FROZEN. Reproduce
its geometry EXACTLY, one for one:
- the slab: same trapezoid, same angles, same size, same position
- every rack, the shelving, the sign, the pole: same position, same
  size, same proportions, same overlaps
- Ed: same pose, same position, same scale, goggles on, temple
  cable, no controller
- every floor prop, every cable run: same place, same shape
You are FORBIDDEN from moving, resizing, rotating, adding,
removing, or reinterpreting ANY object. You are FORBIDDEN from
improving the composition. One object out of place = REJECTION.
The layout question is SETTLED. Your ONLY task is the PAINT.

## THE PAINT - the one thing you change

Repaint the master at radically LOWER detail, flat and chunky:
- EXACTLY 15 colors, nothing else (#00ff00 background key only):
  #020307 #111923 #2b3741 #5a5a58 #9b9a98 #f2f1ee
  #4c2815 #6e3d20 #8f5a2e #96795a
  #dc7707 #f5b749 #8f2f1f #8faf6f #2f7f78
  Scoped: #8f2f1f only Ed's hair, #8faf6f only his goggle lenses,
  #2f7f78 only LED dots, never on screens.
- FLATNESS ABSOLUTE: zero stipple, zero mottle, zero weave, zero
  noise, zero gradients, zero antialiasing. Every color region is
  one flat chunk, blocks never smaller than 4x4 px.
- A surface = 2-3 flat tones, hard edges, near-black outlines.
- The rug = 3 flat tones in LARGE plain rectangles, a simple
  border band, fringe dots. No ornament, no motif rows.
- Wood = 3 flat tones, seams as single dark lines. Rust = one or
  two flat patches, never speckle.
- A dead screen = one dark tone + one flat diagonal glare band.
  The lit CRT = flat two-tone horizontal bands, white-grey only.
- Fewer marks per section everywhere: if a panel had ten details,
  paint three. LOW DETAIL is the goal.

Call image_gen once to GENERATE at 1536x1024. Copy the result
unmodified to /Users/sospedra/labs/sospedra.me/tmp/bazaar3/master-run-20260728/r20-console/gen10-p3.png and print GENERATED=<path>.
